#spring:
#  data:
#    mongodb:
#      host: auth-mongodb
#      username: user
#      password: ${MONGODB_PASSWORD}
#      database: piggymetrics
#      port: 27017
#
server:
#  servlet:
#    context-path: /api/auth
  port: 5000

spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://mysql:3306/student_record_system?createDatabaseIfNotExist=true
    username: root
    password: root
  #druid datasource
  jpa:
    generate-ddl: true
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    hibernate:
      ddl-auto: update
